import React from 'react';

import './header.styles.scss';

const Header = () => {
    return (
        <header className="header">
            <h1 className="heading-1">Battle Simulator</h1>
        </header>
    );
};

export default Header;